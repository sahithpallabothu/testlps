import { AllowNumericsDirective } from './allow-numerics.directive';

describe('AllowNumericsDirective', () => {
  it('should create an instance', () => {
    const directive = new AllowNumericsDirective();
    expect(directive).toBeTruthy();
  });
});
